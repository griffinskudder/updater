# Environment Configuration Example for Updater Service
# Copy to .env.dev, .env.staging, or .env.prod and customize

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Configuration section to use from security-examples.yaml
UPDATER_CONFIG_SECTION=development

# Server settings
UPDATER_PORT=8080
UPDATER_LOG_LEVEL=info

# =============================================================================
# DEVELOPMENT ENVIRONMENT
# =============================================================================

# Development API Keys (32+ character secure keys)
DEV_ADMIN_API_KEY=dev-admin-key-12345678901234567890abcdef
DEV_READ_API_KEY=dev-read-key-09876543210987654321fedcba

# =============================================================================
# STAGING ENVIRONMENT
# =============================================================================

# Staging API Keys (use secure random values)
# STAGING_ADMIN_API_KEY=your-secure-staging-admin-key-here
# STAGING_RELEASE_API_KEY=your-secure-staging-release-key-here
# STAGING_READONLY_API_KEY=your-secure-staging-readonly-key-here

# =============================================================================
# PRODUCTION ENVIRONMENT
# =============================================================================

# Production API Keys (NEVER use example values in production!)
# Generate with: openssl rand -base64 32
# PRODUCTION_ADMIN_API_KEY=your-secure-production-admin-key
# PRODUCTION_RELEASE_API_KEY=your-secure-production-release-key
# PRODUCTION_MONITORING_API_KEY=your-secure-production-monitoring-key

# Production Database
# DATABASE_URL=postgres://username:password@hostname:port/database
# DATABASE_ENCRYPTION_KEY=your-database-encryption-key

# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================

# Docker registry for image builds
DOCKER_REGISTRY=localhost
IMAGE_NAME=updater
VERSION=latest

# =============================================================================
# TLS/SSL CONFIGURATION
# =============================================================================

# TLS certificate paths
# TLS_CERT_FILE=/etc/ssl/certs/updater.pem
# TLS_KEY_FILE=/etc/ssl/private/updater.key

# =============================================================================
# MONITORING AND LOGGING
# =============================================================================

# Log configuration
LOG_FORMAT=json
LOG_FILE=/var/log/updater/updater.log

# Monitoring endpoints
# METRICS_ENDPOINT=http://prometheus:9090
# ALERTMANAGER_ENDPOINT=http://alertmanager:9093

# =============================================================================
# SECURITY SETTINGS
# =============================================================================

# Rate limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS_PER_MINUTE=60

# CORS settings
CORS_ENABLED=true
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://api.yourdomain.com

# Security headers
SECURITY_HEADERS_ENABLED=true

# =============================================================================
# EXTERNAL SERVICES
# =============================================================================

# CDN configuration
# CDN_URL=https://cdn.yourdomain.com
# CDN_API_KEY=your-cdn-api-key

# External storage
# S3_BUCKET=your-s3-bucket
# S3_REGION=us-west-2
# S3_ACCESS_KEY_ID=your-access-key
# S3_SECRET_ACCESS_KEY=your-secret-key

# =============================================================================
# BACKUP AND RECOVERY
# =============================================================================

# Backup configuration
# BACKUP_ENABLED=true
# BACKUP_INTERVAL=24h
# BACKUP_RETENTION=30d
# BACKUP_S3_BUCKET=your-backup-bucket

# =============================================================================
# DEVELOPMENT HELPERS
# =============================================================================

# Development mode settings (disable in production)
DEBUG_MODE=false
ENABLE_PROFILING=false
ENABLE_METRICS=true

# Testing configuration
TEST_DATABASE_URL=sqlite://test.db
TEST_API_KEY=test-key-for-automated-tests

# =============================================================================
# IMPORTANT SECURITY NOTES
# =============================================================================

# 1. Never commit real secrets to version control
# 2. Use environment-specific .env files (.env.dev, .env.prod, etc.)
# 3. Generate API keys with: openssl rand -base64 32
# 4. Rotate keys regularly (every 90 days for production)
# 5. Use secrets management systems in production (AWS Secrets Manager, etc.)
# 6. Set proper file permissions: chmod 600 .env.prod
# 7. Add .env.* to .gitignore to prevent accidental commits

# =============================================================================
# KEY GENERATION COMMANDS
# =============================================================================

# Generate secure API keys:
# openssl rand -base64 32
# head -c 32 /dev/urandom | base64
# python -c "import secrets; print(secrets.token_urlsafe(32))"

# Generate encryption keys:
# openssl rand -hex 32

# Generate JWT secrets:
# openssl rand -base64 64